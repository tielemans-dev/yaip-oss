import type { CountryProfile } from "../country-profile"

export const usProfile: CountryProfile = {
  countryCode: "US",
  defaultLocale: "en-US",
  defaultCurrency: "USD",
  taxRegime: "us_sales_tax",
  requiresSellerTaxId: false,
}
