import type { CountryProfile } from "../country-profile"

export const dkProfile: CountryProfile = {
  countryCode: "DK",
  defaultLocale: "da-DK",
  defaultCurrency: "DKK",
  taxRegime: "eu_vat",
  requiresSellerTaxId: true,
}
