import type { CountryProfile } from "../country-profile"

export const euBaselineProfile: CountryProfile = {
  countryCode: "EU",
  defaultLocale: "en-IE",
  defaultCurrency: "EUR",
  taxRegime: "eu_vat",
  requiresSellerTaxId: true,
}
